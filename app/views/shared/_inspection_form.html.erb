<div class ="center">
	<h1>Transaction Found!</h1>
</div>

<div class="row">
  <div class="col-md-6">
    <%= render :partial => 'shared/quote_info', :locals => {:quote => quote} %>
  </div>
</div>

<%= form_for(inspection, :html => { :role => "form", :class => "form-horizontal", :multipart => true}) do |f| %>
  
  <%= render 'shared/error_messages', object: f.object %>
  
  <% Inspection::INSPECTION_TYPES.each do |name| %>
    <h1> <%=name.titleize %> </h1>
    <div class="form-group">
      <% Inspection::INSPECTION_VALUES.each do |text, value| %>
        <%= f.radio_button name, value %>
        <%= f.label name, text %>
      <% end %>
    </div>
  <% end %> 

  <div class="form-group">
    <%= f.label :additional_info, "Any Additional Info:" %>
    <%= f.text_area :additional_info %>
  </div>

  <%= f.submit "Submit", class: "btn btn-large btn-primary" %>
<% end %>





