<p>
  <strong>Make:</strong>
  <%# make = Make.find(@bike.make_id) %>
  <% make = Make.find(@bike.make_id) %>
  <%= make.name %>
  <strong>Model:</strong>
  <% model_temp = Model.find(@bike.model_id) %>
  <%= model_temp.name %>
  <strong>Year:</strong>
  <% year = Year.find_by(:year_value => @bike.year_id) %>
  <%= year.year_value %>
  <br>
  <p> Depending on its condition, ReRide will purchase your bike for a price up to 
      <strong> <%= number_to_currency((@bike.like_new_value)*0.6) %></strong>.
  <%#= image_tag @bike.side_picture.url(:medium) %>
</p>

<%= form_for(@customer, :html => { :role => "form", :class => "form-horizontal", :multipart => true}) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.fields_for :quotes do |g| %>

        <div class="form-group">
          <%= g.label :side_picture, 'Picture of Bike (from the side)' %>
          <%= g.file_field :side_picture %>
        </div>

        <div class="form-group">
          <%= g.label :front_picture, 'Picture of Bike (from the front)' %>
          <%= g.file_field :front_picture %>
        </div>
      <% end %>

      
      <div class="form-group">
        <%= f.label :first_name, 'First Name' %>
        <%= f.text_field :first_name, :class => "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :last_name, 'Last Name' %>
        <%= f.text_field :last_name, :class => "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :email, 'Email' %>
        <%= f.email_field :email, :class => "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :street_address, 'Street Address' %>
        <%= f.text_field :street_address, :class => "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :city, 'City' %>
        <%= f.text_field :city, :class => "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :state, 'State' %>
        <%= f.select :state, options_for_select(us_states), { :include_blank => true }, {:class => "form-control"} %>
      </div>  

      <div class="form-group">
        <%= f.label :zip_code, 'Zip Code' %>
        <%= f.text_field :zip_code, :class => "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :phone_number, 'Phone Number' %>
        <%= f.telephone_field :phone_number, :class => "form-control" %>
      </div>

      <%= f.submit "Submit", class: "btn btn-large btn-primary" %>
  <% end %>



<%= link_to 'Edit', edit_bike_path(@bike) %> |
<%= link_to 'Back', bikes_path %>
